name: Release
on:
  push:
    tags:
      - "release-*"
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/build.yml
    secrets: inherit
    permissions:
      id-token: write
      attestations: write
      artifact-metadata: write
  release:
    needs:
      - build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v8
        with:
          name: outputs

      - name: Display downloaded files
        run: ls -R

      - name: Release
        uses: softprops/action-gh-release@v3
        with:
          files: |
            apk/**/*.apk
             bundle/**/*.aab

      - name: Upload to Tajgram
        uses: Tajgram/upload-with-mtcute@94b1842cabaa999e2068bab9998e2ec80f92437e
        with:
          api-id: ${{ secrets.API_ID }}
          api-hash: ${{ secrets.API_HASH }}
          bot-token: ${{ secrets.RELEASE_BOT_TOKEN }}
          chat-id: ${{ secrets.RELEASE_CHANNEL_ID }}
          files: apk/**/*.apk
